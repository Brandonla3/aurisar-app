# Character Assets

Drop `base_character.glb` here.

Expected AnimationGroup names (case-insensitive match):
- "Idle"    → matched by /idle/i
- "Walking" → matched by /walk/i

The scene falls back to box-primitive characters if this file is missing.
