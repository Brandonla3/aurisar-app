/**
 * dungeons/index.ts — instanced dungeon definitions. First dungeon lands
 * in P7; the empty array keeps the aggregate/validation shape stable.
 */
import type { DungeonDef } from '../types';

export const DUNGEONS: DungeonDef[] = [];
